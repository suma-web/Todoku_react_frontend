import{NavLink,Outlet}from"react-router-dom";import{useAuth}from"../../contexts/auth";
export const SchoolLayout=()=>{const{user}=useAuth();const links=[{to:"/",label:"ホーム"},{to:"/timeline",label:"連絡"},{to:"/questions",label:"質問"},{to:"/search",label:"検索"}];if(user?.role==="teacher"||user?.role==="admin")links.push({to:"/school-posts/new",label:"連絡作成"});if(user?.role==="admin")links.push({to:"/admin/users",label:"ユーザー"},{to:"/admin/groups",label:"所属"},{to:"/admin/question-categories",label:"質問カテゴリ"});return <div className="min-h-dvh bg-slate-950 text-white"><header className="sticky top-0 z-20 border-b border-slate-800 bg-slate-950/95"><nav className="mx-auto flex max-w-5xl items-center gap-2 overflow-x-auto p-3"><strong className="mr-4 whitespace-nowrap">学校情報基盤</strong>{links.map(link=><NavLink key={link.to} to={link.to} className={({isActive})=>`whitespace-nowrap rounded-full px-4 py-2 ${isActive?"bg-sky-600":"hover:bg-slate-800"}`}>{link.label}</NavLink>)}</nav></header><Outlet/></div>};
